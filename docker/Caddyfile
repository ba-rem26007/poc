# Caddy configuration for 3-Tier TLS Setup (Development & Production poc.d1dev.fr)

poc.d1dev.fr, https://localhost {
    # If request is for localhost, use internal self-signed TLS.
    # Caddy automatically provisions Let's Encrypt certificates for poc.d1dev.fr!

    # EasyAdmin dashboard and Symfony security
    handle /admin* {
        reverse_proxy backend:8000
    }

    handle /login* {
        reverse_proxy backend:8000
    }

    handle /logout* {
        reverse_proxy backend:8000
    }

    # API Platform endpoints & Swagger UI
    handle /api* {
        reverse_proxy backend:8000
    }

    handle /_profiler* {
        reverse_proxy backend:8000
    }

    handle /_wdt* {
        reverse_proxy backend:8000
    }

    # React Frontend application
    handle {
        reverse_proxy frontend:5173
    }
}
